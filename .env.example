# dbt_olist_project Environment Variables
# Copy this file to .env and fill in your actual values
# The .env file is gitignored for security

# Snowflake Connection Details
DBT_ACCOUNT=your-snowflake-account
DBT_USER=your-username
DBT_PASSWORD=your-password
DBT_ROLE=your-role
DBT_DATABASE=your-database
DBT_WAREHOUSE=your-warehouse

# Environment-specific schemas
DBT_DEV_SCHEMA=DBT_DEV
DBT_PROD_SCHEMA=DBT_PROD

# GitHub Secrets Mapping for CI/CD:
# In GitHub repository settings > Secrets and variables > Actions, add:
# - DBT_ACCOUNT (maps to DBT_ACCOUNT)
# - DBT_USER (maps to DBT_USER)
# - DBT_PASSWORD (maps to DBT_PASSWORD)
# - DBT_ROLE (maps to DBT_ROLE)
# - DBT_DATABASE (maps to DBT_DATABASE)
# - DBT_WAREHOUSE (maps to DBT_WAREHOUSE)
# - DBT_DEV_SCHEMA (maps to DBT_DEV_SCHEMA)
# - DBT_PROD_SCHEMA (maps to DBT_PROD_SCHEMA)
